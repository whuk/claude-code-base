---
description: "~/.claude/plans에 쌓인 플랜 파일을 목록으로 확인한 뒤 사용자 승인을 받아 삭제한다. 인자로 일수를 주면 그 일수를 초과한 플랜을 자동 선별한다."
argument-hint: "[일수] (선택, 예: 90)"
user-invocable: true
allowed-tools: Bash, Read, AskUserQuestion
---

# /rw:plan:plan_clean — 오래된 플랜 정리

플랜 모드에서 생성된 플랜 파일은 `~/.claude/plans`에 계속 쌓인다. 이 커맨드는 그중 오래되었거나 더 이상 필요 없는 플랜을 사용자 확인을 거쳐 삭제한다. `~/.claude/plans`는 모든 프로젝트가 공유하는 전역 디렉토리이므로, 현재 프로젝트 것만이 아니라 전역 전체를 대상으로 한다.

## 1단계: 사전 검증

1. `~/.claude/plans` 디렉토리가 존재하는지 확인한다. 없으면 다음을 출력하고 **중단**한다:
   > `~/.claude/plans` 디렉토리가 없습니다. 정리할 플랜이 없습니다.

2. `$ARGUMENTS`가 비어있지 않은데 숫자가 아니면 다음을 출력하고 **중단**한다:
   > 인자는 일수(숫자)여야 합니다. 예: `/rw:plan:plan_clean 90`
   > 인자 없이 실행하면 전체 목록에서 직접 선택할 수 있습니다.

## 2단계: 플랜 목록 수집

본 플랜은 `{slug}.md`이며, `{slug}.workshop.md`와 `{slug}-agent-*.md`는 그 플랜에 딸린 **부수 파일**이다. 부수 파일을 독립 항목으로 세지 않고 본 플랜에 묶어서 취급한다.

```bash
for f in ~/.claude/plans/*.md; do
  case "$f" in *.workshop.md|*-agent-*.md) continue;; esac
  printf '%s\t%s\t%s\n' \
    "$(date -r "$f" +%Y-%m-%d)" \
    "$(basename "$f" .md)" \
    "$(head -20 "$f" | grep -m1 '^#' | sed 's/^#*[[:space:]]*//')"
done | sort
```

각 플랜이 생성된 프로젝트는 트랜스크립트에서 역추적한다(slug당 0.1초 수준):

```bash
grep -rl "\"slug\":\"{slug}\"" ~/.claude/projects --include='*.jsonl' 2>/dev/null | head -1
```

경로의 디렉토리명이 곧 프로젝트다. 찾지 못하면 `(알 수 없음)`으로 표시한다.

## 3단계: 삭제 후보 선별

**오늘 수정된 플랜은 현재 진행 중인 세션의 것일 수 있으므로 두 경로 모두에서 후보로 삼지 않는다.** 사용자가 번호로 명시적으로 지목한 경우에만 포함하되, 진행 중인 세션의 플랜일 수 있다는 점을 함께 알린다.

후보가 하나도 없으면 그 사실을 알리고 중단한다.

### 경로 A: 인자로 일수를 받은 경우

최종 수정 시각이 그 일수를 초과한 플랜만 후보로 삼는다.

```bash
find ~/.claude/plans -maxdepth 1 -name '*.md' -mtime +{일수}
```

### 경로 B: 인자가 없는 경우

2단계에서 수집한 전체 목록에 번호를 붙여 제시하고, 어떤 플랜을 삭제할지 사용자에게 묻는다.

## 4단계: 확인

삭제 후보를 표로 제시한다. 열은 번호, 최종 수정일, 제목, 프로젝트, 파일 수(부수 파일 포함), 크기로 구성한다. 합계 파일 수와 확보될 용량을 함께 보여준다. 이때 각 후보의 최종 수정 시각(`stat -f %m`)을 기록해 둔다 — 5단계에서 대조할 기준값이다.

**사용자가 명시적으로 승인하기 전에는 어떤 파일도 삭제하지 않는다.** 인자로 일수를 받은 경우에도 자동 삭제하지 않고 반드시 확인을 거친다.

## 5단계: 삭제

`~/.claude/plans`는 모든 프로젝트가 공유하는 전역 디렉토리이고, 2단계 수집부터 여기까지는 사용자 확인 대기가 끼어 있다. 그동안 다른 세션이 플랜을 새로 쓰거나 갱신했을 수 있으므로 **삭제 직전에 후보별로 최종 수정 시각을 다시 확인한다.**

1. 각 후보의 현재 수정 시각을 4단계에서 기록한 값과 대조한다:
   ```bash
   stat -f %m ~/.claude/plans/{slug}.md
   ```
2. 값이 달라졌으면 그 후보는 **건너뛴다.** 승인 이후 갱신됐다는 뜻이며, 진행 중인 세션의 플랜일 수 있다. 건너뛴 항목과 사유를 결과 보고에 남긴다.
3. 값이 같은 후보만, 본 플랜과 부수 파일을 함께 삭제한다. 본 플랜만 지워 부수 파일이 고아로 남지 않게 한다.
   ```bash
   rm -f ~/.claude/plans/{slug}.md \
         ~/.claude/plans/{slug}.workshop.md \
         ~/.claude/plans/{slug}-agent-*.md
   ```

## 결과 보고

- 삭제한 플랜 수와 실제 파일 수
- 확보한 용량
- 삭제한 플랜 목록(날짜, 제목)
- 5단계 2항으로 건너뛴 항목이 있으면 그 목록과 사유(승인 이후 갱신됨)
- `~/.claude/plans`에 남은 플랜 수

## 하지 말아야 할 것

- 사용자 승인 없이 삭제하기 (인자로 일수를 받았더라도 확인은 반드시 거친다)
- 오늘 수정된 플랜을 확인 없이 후보에 포함하기 (진행 중 세션의 플랜일 수 있다)
- 승인 이후 갱신된 플랜을 그대로 삭제하기 (5단계에서 수정 시각을 대조하고, 달라졌으면 건너뛴다)
- 본 플랜만 삭제하고 부수 파일(`{slug}.workshop.md`, `{slug}-agent-*.md`)을 남기기
- `~/.claude/plans` 밖의 경로를 건드리기
- 인자가 숫자가 아닌데 임의로 해석해서 진행하기
- 삭제 전에 플랜 내용을 수정하거나 다른 곳으로 옮기기
